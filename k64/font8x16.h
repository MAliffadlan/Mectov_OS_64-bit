#ifndef FONT8X16_H
#define FONT8X16_H

/* 8x16 bitmap font, ported verbatim (data only) from the 32-bit tree.
 * 256 glyphs x 16 rows; row bit 7 = leftmost pixel. */
extern unsigned char font8x16_data[256][16];

#endif
